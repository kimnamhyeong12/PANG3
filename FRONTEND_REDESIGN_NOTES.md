# 외근도우미 프론트 개편본

## 변경 범위

- 지도 화면(`MapScreen.js`)의 검색, 방문지, 우선순위, 경로 최적화 로직은 유지했습니다.
- 로그인, 회원가입, 홈, 보고서, 그룹, 그룹 상세, 그룹 생성, 초대, 다운로드 화면을 새 디자인으로 개편했습니다.
- 담당자 지정, 팀 업무 현황, 분석, 현장 기록 화면도 새 색상 체계로 통일했습니다.
- 회원가입과 설정에 개인 근무 시·도 선택을 추가했습니다.
- 그룹 생성에 부산 구·군 활동지역 선택을 추가했습니다.
- 그룹 상세에 `공공데이터 배정` 진입 버튼을 추가했습니다.
- 공공데이터 배정 화면에 SGIS 행정동 경계, 경계 선택/확대, 가로수·맨홀뚜껑·버스정류장 예시 선택, 담당자 선택 UI를 추가했습니다.

## 공공데이터 연동 상태

현재 공공데이터 항목은 화면 흐름을 확인하기 위한 예시 데이터입니다. 실제 공공데이터 API 담당자가 API를 연결할 때 `PublicDataAssignmentScreen.js`의 `mockItems`를 API 조회 결과로 교체하면 됩니다.

## 실행 확인

프론트는 아래 명령으로 Android 번들 생성까지 확인했습니다.

```bash
cd frontend
npm install
npx expo export --platform android
```

백엔드는 기존처럼 `application.yml`과 SGIS 환경변수를 설정한 뒤 실행합니다. `spring.jpa.hibernate.ddl-auto=update` 환경에서는 새 지역 컬럼이 자동 반영됩니다.
